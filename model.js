let recipeContainer, recipeName, recipeIngredients, recipeInstructions;

function renderRecipe( _elementId, _recipeObject ){
    //webPage = document.body();
    recipeContainer = document.getElementById( _elementId );

}